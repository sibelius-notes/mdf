---
title: "CO 450/650: Combinatorial Optimization"
prof: "Prof. Ricardo Fukasawa"
---

> These notes are based on [Felix Zhou's course notes](https://felix-zhou.com/).
> Additional explanations, visualizations, and examples have been added for clarity.

Combinatorial optimization asks: given a finite set of objects each carrying a cost, find the subset satisfying some structural constraint that minimizes (or maximizes) total cost. The course develops both the algorithms and the polyhedral theory that explains *why* those algorithms work. The thread running through the whole course is a fruitful dialogue between combinatorial structure and linear programming duality.

---

## Chapter 1: Spanning Trees and Greedy Algorithms

### 1.1 Spanning Trees

A **spanning tree** is the minimal connected spanning subgraph of a graph. Intuitively, it is a "backbone" that keeps all vertices reachable while discarding redundant edges.

<div class="definition">
<strong>Definition</strong> (Spanning Tree). A subgraph \(T\) of \(G = (V, E)\) is a <strong>spanning tree</strong> if
<ol>
<li>\(V(T) = V(G)\) — it spans all vertices;</li>
<li>\(T\) is connected;</li>
<li>\(T\) is acyclic.</li>
</ol>
</div>

![Spanning tree highlighted in red on a 6-node graph](/pics/co450/spanning-tree-def.png)

These three conditions are highly redundant — any two of them, together with \(|E(T)| = n-1\), characterize a spanning tree. The following theorem captures all the equivalent characterizations.

<div class="theorem">
<strong>Theorem 1.1.1</strong> (Equivalent Characterizations). Let \(G\) be connected and \(T\) a subgraph with \(V(T)=V(G)\). The following are equivalent:
<ul>
<li>(a) \(T\) is a spanning tree;</li>
<li>(b) \(T\) is minimally connected;</li>
<li>(c) \(T\) is maximally acyclic;</li>
<li>(d) \(T\) is connected and \(|E(T)| = n-1\);</li>
<li>(e) \(T\) is acyclic and \(|E(T)| = n-1\);</li>
<li>(f) For all \(u,v \in V\), there is a unique \(uv\)-path in \(T\).</li>
</ul>
</div>

The equivalence of (b) and (c) is particularly elegant: a spanning tree is simultaneously the *fewest* edges needed for connectivity, and the *most* edges before a cycle appears.

### 1.2 Minimum Spanning Trees

Given a connected graph \(G\) with edge costs \(c : E \to \mathbb{R}\), the **minimum spanning tree (MST)** problem asks for a spanning tree \(T\) minimizing \(c(T) := \sum_{e \in E(T)} c_e\).

![Weighted graph with MST edges highlighted in green](/pics/co450/mst-example.png)

The key optimality characterization for MSTs is the **cycle property** (condition b) and the **cut property** (condition c):

<div class="theorem">
<strong>Theorem 1.2.1</strong> (MST Optimality Conditions). Let \(G\) be connected, \(c : E \to \mathbb{R}\), and \(T\) a spanning tree. The following are equivalent:
<ul>
<li>(a) \(T\) is a MST;</li>
<li>(b) <strong>Cycle property:</strong> For all \(uv \in E \setminus E(T)\), every edge \(e\) on the unique path \(T_{u,v}\) satisfies \(c_e \leq c_{uv}\);</li>
<li>(c) <strong>Cut property:</strong> For all \(e \in E(T)\), if \(T_1, T_2\) are the two components of \(T - e\), then \(e\) is a minimum-cost edge in the cut \(\delta(T_1)\).</li>
</ul>
</div>

<div class="proof">
<strong>Proof.</strong>
\(\neg(b) \Rightarrow \neg(a)\): Suppose \(uv \in E \setminus E(T)\) and \(e \in T_{u,v}\) with \(c_e > c_{uv}\). Then \(T + uv - e\) is a spanning tree of strictly smaller cost.

\(\neg(c) \Rightarrow \neg(b)\): If \(e \in T\) has a cheaper edge \(uv\) in the same cut, then \(e\) lies on \(T_{u,v}\) but \(c_e > c_{uv}\).

\((c) \Rightarrow (a)\): Suppose \(T\) satisfies (c) and \(T^*\) is a MST maximizing \(|E(T) \cap E(T^*)|.\) If this intersection has \(n-1\) edges, \(T = T^*\). Otherwise, pick \(uv \in E(T) \setminus E(T^*)\). The path \(T^*_{u,v}\) contains some edge \(e^* \in \delta(T_1)\). By (c), \(c_{uv} \leq c_{e^*}\), so \(T^* - e^* + uv\) is a spanning tree whose edge-intersection with \(T\) is larger — contradicting maximality. \(\square\)
</div>

### 1.3 Kruskal's Algorithm

The cycle property suggests a simple greedy algorithm: sort edges by cost and add each edge if it does not create a cycle.

<div class="key-box">
<strong>Algorithm: Kruskal's MST</strong>

```
H := (V, {})
Sort edges: c(e₁) ≤ c(e₂) ≤ ... ≤ c(eₘ)
for each edge uv in sorted order:
    if component(u) ≠ component(v):
        H.add(uv)
return H
```

**Data structure:** Union-Find for \(O(m \log m)\) sorting plus \(O(m \alpha(m,n))\) union-find operations, giving near-linear total time. Here \(\alpha\) is the inverse Ackermann function, effectively constant for all practical inputs.
</div>

The algorithm proceeds step by step, adding the cheapest safe edge at each stage:

![Kruskal Step 1: first edge added](/pics/co450/kruskal-step1.png)

![Kruskal Step 2: three edges added, one cycle rejected](/pics/co450/kruskal-step2.png)

![Kruskal Step 3: complete MST formed](/pics/co450/kruskal-step3.png)

![Animated: Kruskal's algorithm on a 7-node weighted graph](/pics/co450/kruskal.gif)

**Correctness.** Suppose Kruskal's does not return an MST. Since it does return a spanning tree, there exists \(uv \in E \setminus E(H)\) and \(e \in H_{u,v}\) with \(c_{uv} < c_e\). But \(uv\) was processed before \(e\) in the sorted order and would have been added (since the endpoints were in different components at that point), a contradiction.

### 1.4 LP Relaxation and Complementary Slackness

An elegant alternative proof uses LP duality. The MST LP (PST) is:
\[
\min \; c^T x \quad \text{s.t.} \quad x(E) = n - 1, \quad x(F) \leq n - \kappa(F) \; \forall F \subseteq E, \quad x \geq 0
\]
where \(\kappa(F)\) is the number of connected components induced by edge subset \(F\). Its dual (DST) is:
\[
\max \sum_{F \subseteq E} (n - \kappa(F)) y_F \quad \text{s.t.} \quad \sum_{F : e \in F} y_F \leq c_e \; \forall e \in E, \quad y_F \leq 0 \; \forall F \subsetneq E.
\]

**Proposition 1.4.1.** The characteristic vector of Kruskal's spanning tree is optimal for (PST).

The dual certificate is constructed using the edge ordering from Kruskal: set \(\bar{y}_{E_i} = c_{e_i} - c_{e_{i+1}}\) (a non-positive "step-down") and \(\bar{y}_E = c_{e_m}\). A telescoping argument shows the dual edge constraints are tight for each \(e_i\), and the primal constraints \(x(E_i) = n - \kappa(E_i)\) confirm complementary slackness, establishing simultaneous optimality.

### 1.5 Prim's Algorithm

An alternative to Kruskal's is Prim's algorithm, which grows a single tree from a root vertex by always adding the cheapest edge crossing the current tree boundary (the **frontier**).

![Prim's algorithm: green = tree, orange = frontier edges](/pics/co450/prim-steps.png)

Prim's algorithm is especially efficient with priority queues: \(O(m + n \log n)\) using Fibonacci heaps. Both Kruskal's and Prim's are instances of a more general **greedy paradigm** — one that works exactly when the underlying structure is a **matroid**.

### 1.6 Greedy Algorithms and the Maximum Forest

The **maximum forest problem** generalizes MST: given edge weights \(c : E \to \mathbb{R}\), find an acyclic subgraph \(F\) maximizing \(\sum_{e \in F} c(e)\).

This reduces to MST: replace edge costs \(c_e\) by \(c'_e = -(c_e - M)\) for large \(M\), making all costs positive, then find an MST with respect to \(c'\). The greedy algorithm for maximum forests simply sorts edges and adds each if it does not create a cycle, stopping when costs become non-positive.

---

## Chapter 2: Matroids

### 2.1 Definition and Motivation

Spanning forests are acyclic subgraphs — they satisfy a hereditary closure property (subsets of forests are forests) and an augmentation property (between two forests of different sizes, one can always be augmented from the other). This abstract structure is precisely a **matroid**.

<div class="definition">
<strong>Definition</strong> (Matroid). A <strong>matroid</strong> is a pair \(M = (S, \mathcal{I})\) where \(S\) is a finite ground set and \(\mathcal{I} \subseteq 2^S\) is a collection of <strong>independent sets</strong> satisfying:
<ul>
<li>(M1) <strong>Non-emptiness:</strong> \(\emptyset \in \mathcal{I}\);</li>
<li>(M2) <strong>Hereditary:</strong> If \(F \in \mathcal{I}\) and \(F' \subseteq F\), then \(F' \in \mathcal{I}\);</li>
<li>(M3) <strong>Augmentation:</strong> For all \(A \subseteq S\), every maximal independent set contained in \(A\) (basis of \(A\)) has the same cardinality.</li>
</ul>
</div>

![Graphic matroid: forests of a graph form the independent sets](/pics/co450/matroid-def.png)

The three canonical examples are:

**Graphic (Forest) Matroid.** For a graph \(G = (V, E)\), let \(\mathcal{I}\) be the set of all forests. Then \(M = (E, \mathcal{I})\) is a matroid. The bases are exactly the spanning trees.

**Uniform Matroid \(U_{r,n}\).** Ground set \(S = [n]\), independent sets = all subsets of size at most \(r\). Every set of \(r\) elements is a basis.

![Uniform matroid U(2,4): all 2-element subsets of {1,2,3,4}](/pics/co450/uniform-matroid.png)

**Linear Matroid.** Given a matrix \(N \in \mathbb{F}^{m \times n}\), let \(\mathcal{I}\) = \{column index sets that are linearly independent\}. This is a matroid over \(S = [n]\).

### 2.2 Independence Systems and Rank

If \(M = (S, \mathcal{I})\) satisfies only (M1) and (M2), it is an **independence system**. Elements of \(\mathcal{I}\) are **independent sets**; minimal dependent sets are **circuits**.

<div class="definition">
<strong>Definition</strong> (Rank). The <strong>rank</strong> of \(A \subseteq S\) is \(r(A) := \max\{|B| : B \subseteq A, B \in \mathcal{I}\}\). The <strong>lower rank</strong> is \(\rho(A) := \min\{|B| : B \text{ is a basis of } A\}\).
</div>

<div class="theorem"><strong>Proposition 2.1.4.</strong> \(M = (S, \mathcal{I})\) is a matroid if and only if \(\rho(A) = r(A)\) for all \(A \subseteq S\).</div>

This says a matroid is exactly the independence system where all bases of every set have the same size — there are no "short" bases.

### 2.3 The Greedy Algorithm on Matroids

<div class="theorem">
<strong>Theorem 2.2.1</strong> (Rado–Edmonds). Let \(M = (S, \mathcal{I})\) be a matroid and \(c \in \mathbb{R}_+^S\). The greedy algorithm — repeatedly add the maximum weight element that keeps the set independent — finds a maximum weight independent set.
</div>

<div class="theorem">
<strong>Theorem 2.2.2</strong> (Rado–Edmonds Converse). Let \(M = (S, \mathcal{I})\) be an independence system. The greedy algorithm finds an optimal independent set for all \(c \in \mathbb{R}_+^S\) if and only if \(M\) is a matroid.
</div>

<div class="proof">
<strong>Proof</strong> (\(\Rightarrow\)). Suppose \(M\) is not a matroid. Then some \(A \subseteq S\) has bases \(A_1, A_2\) with \(|A_1| < |A_2|\). Set costs \(c(e) = 1\) for \(e \in A_1\), \(c(e) = 1-\epsilon\) for \(e \in A_2 \setminus A_1\), and \(c(e) = 0\) otherwise. Choosing \(\epsilon\) small enough so that \((1-\epsilon)|A_2| > |A_1|\) makes \(A_2\) the true optimum, but greedy constructs \(A_1\) — a failure. \(\square\)
</div>

Even when the greedy fails, it cannot be arbitrarily bad:

<div class="theorem">
<strong>Theorem 2.2.3</strong> (Jenkins). For any independence system \((S, \mathcal{I})\):
\[
g(S, \mathcal{I}) \geq q(S, \mathcal{I}) \cdot \mathrm{OPT}(S, \mathcal{I}),
\]
where the <strong>rank quotient</strong> is \(q(S, \mathcal{I}) := \min_{A : r(A) \neq 0} \frac{\rho(A)}{r(A)} \in [0,1]\). For matroids, \(q = 1\).
</div>

### 2.4 Alternative Characterizations

**Exchange property (M3').** \(M\) is a matroid if and only if: for all \(X, Y \in \mathcal{I}\) with \(|X| > |Y|\), there exists \(x \in X \setminus Y\) such that \(Y \cup \{x\} \in \mathcal{I}\).

This is the more common definition in textbooks. It is equivalent to (M3): any smaller independent set can always be augmented from a larger one.

**Circuit characterization.** A collection \(\mathcal{C} \subseteq 2^S\) is the set of circuits of a matroid if and only if:
- (C1) \(\emptyset \notin \mathcal{C}\);
- (C2) No circuit is a proper subset of another (anti-chain);
- (C3) **Circuit elimination:** If \(C_1 \neq C_2 \in \mathcal{C}\) and \(e \in C_1 \cap C_2\), there exists a circuit \(C \subseteq (C_1 \cup C_2) \setminus \{e\}\).

**Basis exchange.** \(\mathcal{B} \subseteq 2^S\) is the set of bases of a matroid if and only if \(\mathcal{B} \neq \emptyset\) and: for any \(B_1, B_2 \in \mathcal{B}\) and \(x \in B_1 \setminus B_2\), there exists \(y \in B_2 \setminus B_1\) with \((B_1 \setminus \{x\}) \cup \{y\} \in \mathcal{B}\).

### 2.5 Polymatroids and Submodularity

The rank function of a matroid is **submodular**:

<div class="definition">
<strong>Definition</strong> (Submodular Function). \(f : 2^S \to \mathbb{R}\) is <strong>submodular</strong> if for all \(A, B \subseteq S\):
\[
f(A) + f(B) \geq f(A \cap B) + f(A \cup B).
\]
</div>

![Submodular rank function: three-panel visualization of r(A), r(B), r(A∩B), r(A∪B)](/pics/co450/matroid-rank.png)

<div class="theorem">
<strong>Proposition 3.2.2.</strong> The rank function of any matroid is submodular.
</div>

<div class="proof">
<strong>Proof.</strong> Let \(J_\cap\) be a basis of \(A \cap B\), extend to \(J_B\) (basis of \(B\)), then to \(J_\cup\) (basis of \(A \cup B\)). Put \(J' = J_\cup \setminus (J_B \setminus J_\cap)\). Then \(J' \in \mathcal{I}\) and \(J' \subseteq A\), giving:
\[
r(A) + r(B) \geq |J'| + |J_B| = r(A \cup B) + r(A \cap B). \quad \square
\]
</div>

A **polymatroid** is the polytope \(\{x \in \mathbb{R}^S : x(A) \leq f(A) \; \forall A \subseteq S, \; x \geq 0\}\) for a monotone submodular \(f\) with \(f(\emptyset) = 0\). The LP \(\max\{c^T x : x \text{ in polymatroid}\}\) is solved exactly by the greedy algorithm — and this optimality is proven via complementary slackness with a dual solution constructed from the greedy ordering.

---

## Chapter 3: Matroid Constructions and Intersection

### 3.1 Operations on Matroids

Given a matroid \(M = (S, \mathcal{I})\), several operations yield new matroids:

- **Deletion** \(M \setminus B\): restrict to ground set \(S \setminus B\), same independence.
- **Truncation** to rank \(k\): \(\mathcal{I}' = \{A \in \mathcal{I} : |A| \leq k\}\).
- **Disjoint Union**: if \(M_i = (S_i, \mathcal{I}_i)\) with \(S_i\) pairwise disjoint, \(M_1 \oplus \cdots \oplus M_k\) has ground set \(\bigcup S_i\) and \(A \in \mathcal{I}\) iff \(A \cap S_i \in \mathcal{I}_i\) for all \(i\).
- **Contraction** \(M / B\): fix a basis \(J\) of \(B\); the contracted matroid has ground set \(S \setminus B\) and \(A \in \mathcal{I}'\) iff \(A \cup J \in \mathcal{I}\). Importantly: \(r_{M/B}(A) = r_M(A \cup B) - r_M(B)\).
- **Dual** \(M^* = (S, \mathcal{I}^*)\): \(A \in \mathcal{I}^*\) iff \(r(S \setminus A) = r(S)\), with rank \(r^*(A) = |A| + r_M(S \setminus A) - r_M(S)\). For the graphic matroid of a planar graph, the dual matroid is the graphic matroid of the planar dual.

### 3.2 Matroid Intersection

The matroid intersection problem is one of the deepest results in combinatorial optimization:

<div class="theorem">
<strong>Theorem 5.1.2</strong> (Edmonds Matroid Intersection Theorem). For matroids \(M_1 = (S, \mathcal{I}_1)\) and \(M_2 = (S, \mathcal{I}_2)\):
\[
\max\{|J| : J \in \mathcal{I}_1 \cap \mathcal{I}_2\} = \min_{A \subseteq S} \{r_1(A) + r_2(S \setminus A)\}.
\]
</div>

This is a min-max theorem: the maximum common independent set has the same size as the minimum "partition certificate." As a special case, bipartite matching is matroid intersection (two partition matroids), and the theorem reduces to König's theorem.

**Algorithm.** Build an auxiliary directed graph on \(S \cup \{r, s\}\):
- Add arc \(r \to e\) if \(e \notin J\) and \(J \cup \{e\} \in \mathcal{I}_2\);
- Add arc \(e \to s\) if \(e \notin J\) and \(J \cup \{e\} \in \mathcal{I}_1\);
- For \(e \notin J\), \(f \in J\): add arc \(e \to f\) if \(J \cup \{e\} \setminus \{f\} \in \mathcal{I}_1\), and arc \(f \to e\) if \(J \cup \{e\} \setminus \{f\} \in \mathcal{I}_2\).

An \(rs\)-dipath corresponds to an **augmenting path** that increases \(|J \cap \mathcal{I}_1 \cap \mathcal{I}_2|\) by 1. If no such path exists, the reachable set \(U\) from \(r\) witnesses optimality via the min-max formula.

**3-way intersection is NP-hard.** Intersecting three matroids simultaneously is computationally intractable in general, highlighting the sharpness of the two-matroid result.

**Matroid Partitioning.** \(J \subseteq S\) is **partitionable** into \(k\) matroids \(M_1, \ldots, M_k\) if \(J = J_1 \sqcup \cdots \sqcup J_k\) with \(J_i \in \mathcal{I}_i\). The Edmonds–Fulkerson theorem gives:
\[
\max\{|J| : J \text{ partitionable}\} = \min_{A \subseteq S} \left\{|\bar{A}| + \sum_{i=1}^k r_i(A)\right\}.
\]

---

## Chapter 4: Matchings

### 4.1 Basic Definitions

<div class="definition">
<strong>Definition</strong> (Matching). For a graph \(G\), a subset \(M \subseteq E\) is a <strong>matching</strong> if \(|\delta(v) \cap M| \leq 1\) for all \(v \in V\). A vertex is <strong>\(M\)-covered</strong> if it is incident to an edge of \(M\), otherwise <strong>\(M\)-exposed</strong>.
</div>

![Bipartite graph with a matching highlighted in green](/pics/co450/bipartite-matching-def.png)

Let \(\nu(G)\) denote the maximum matching size and \(\tau(G)\) the minimum vertex cover size. The inequality \(\nu(G) \leq \tau(G)\) is immediate (every edge in a matching needs a vertex in the cover). For bipartite graphs, equality holds — this is König's theorem.

### 4.2 Augmenting Paths and Berge's Theorem

<div class="definition">
<strong>Definition</strong> (Alternating and Augmenting Paths). A path \(P : v_1, \ldots, v_k\) is <strong>\(M\)-alternating</strong> if consecutive edges alternate between \(M\) and \(E \setminus M\). It is <strong>\(M\)-augmenting</strong> if \(v_1\) and \(v_k\) are both \(M\)-exposed.
</div>

Taking the symmetric difference \(M' = M \triangle E(P)\) along an augmenting path gives a matching of size \(|M| + 1\).

![Augmenting path in bipartite matching: alternating path with exposed endpoints](/pics/co450/augmenting-path-matching.png)

<div class="theorem">
<strong>Theorem 4.1.1</strong> (Berge). A matching \(M\) is maximum if and only if there is no \(M\)-augmenting path.
</div>

<div class="proof">
<strong>Proof.</strong> (\(\Rightarrow\)) An augmenting path immediately gives a larger matching.

(\(\Leftarrow\)) Suppose \(M'\) is a larger matching. Consider \(G[M \triangle M']\): every vertex has degree at most 2, so it is a disjoint union of alternating paths and cycles. Since \(|M'| > |M|\), at least one path component has more \(M'\)-edges than \(M\)-edges — this is the desired augmenting path. \(\square\)
</div>

### 4.3 Bipartite Matching

For bipartite graphs, the BFS alternating-tree algorithm finds maximum matchings in polynomial time. The correctness relies on Hall's theorem:

<div class="theorem">
<strong>Theorem 4.2.1</strong> (Hall's Marriage Theorem). Let \(G\) be bipartite with sides \(A\) and \(B\). There is a matching saturating all of \(A\) if and only if \(|N(X)| \geq |X|\) for all \(X \subseteq A\).
</div>

The bipartite matching algorithm grows an alternating tree from each exposed vertex. It either finds an augmenting path (improving the matching) or concludes with a "frustrated" tree satisfying \(N(B(T)) = A(T)\) and \(|B(T)| > |A(T)|\) — witnessing a Hall violation and hence no perfect matching.

**König's Theorem.** For bipartite \(G\): \(\nu(G) = \tau(G)\). This follows from the matroid intersection theorem applied to the two partition matroids (degree constraints on each side), or alternatively from the max-flow min-cut theorem.

### 4.4 General Graphs and Blossoms

When odd cycles are present, simple alternating trees can fail: two vertices in \(B(T)\) may be adjacent, creating an odd cycle (a **blossom**).

<div class="definition">
<strong>Definition</strong> (Blossom). A <strong>blossom</strong> is an odd cycle \(C\) in the alternating tree \(T + uv\) where \(u, v \in B(T)\).
</div>

![Non-bipartite graph showing a blossom (odd cycle) with stem](/pics/co450/blossom-example.png)

The key insight of Edmonds' algorithm: shrink the blossom \(C\) to a single supernode \(v_C\) and continue the algorithm in the contracted graph \(G' = G/C\). A matching in \(G'\) can be extended to a matching in \(G\) preserving the number of exposed vertices.

![Blossom contracted to single vertex; augmenting path found in contracted graph](/pics/co450/blossom-contraction.png)

### 4.5 The Blossom Algorithm

<div class="key-box">
<strong>Algorithm: Blossom (Edmonds, 1965)</strong>

```
Initialize M = {}, grow alternating tree T from an exposed vertex r
while edge vw exists with v ∈ B(T), w ∉ A(T):
    if w is M-exposed:
        augment M along path in T ∪ {vw}
        restart with new exposed vertex
    elif w ∉ V(T) (w is M-covered by wx):
        extend T: add w to A(T), x to B(T)
    else (w ∈ B(T)):
        shrink blossom C = unique odd cycle in T + vw
        update G, M, T in contracted graph
if T is frustrated (all neighbors of B(T) are in A(T)):
    output "no perfect matching"
```
</div>

<div class="theorem">
<strong>Theorem 4.3.1.</strong> The blossom algorithm performs \(O(n)\) augmentations, \(O(n^2)\) shrinks, and \(O(m)\) tree extensions. It correctly determines whether \(G\) has a perfect matching and finds one if it exists.
</div>

**Correctness of "no perfect matching."** If the algorithm terminates with a frustrated tree \(T\), then \(N(B(T)) \subseteq A(T)\), so removing \(A(T)\) leaves \(|B(T)| > |A(T)|\) odd components. By Tutte's theorem (below), \(G\) has no perfect matching.

### 4.6 The Tutte-Berge Formula

<div class="theorem">
<strong>Theorem 4.4.1</strong> (Tutte-Berge Formula). For any graph \(G\):
\[
\nu(G) = \frac{1}{2} \min_{A \subseteq V} \{|V| - \mathrm{odd}(G - A) + |A|\},
\]
where \(\mathrm{odd}(G - A)\) is the number of odd connected components of \(G - A\).
</div>

<div class="theorem">
<strong>Theorem 4.4.2</strong> (Tutte's Perfect Matching Theorem). \(G\) has a perfect matching if and only if \(\mathrm{odd}(G - A) \leq |A|\) for all \(A \subseteq V\).
</div>

The proof of the full Tutte-Berge formula is by induction on \(|E|\), considering two cases: one endpoint of an edge is **essential** (covered in every maximum matching), or both are **inessential**. In the latter case, one constructs a tight odd cycle (blossom) and contracts, reducing to a smaller graph.

### 4.7 The Gallai-Edmonds Decomposition

The structure of maximum matchings is completely described by the **Gallai-Edmonds partition** \((B, C, D)\) of \(V\):
- \(B\) = **inessential** vertices (exposed in some maximum matching);
- \(C\) = \(N(B) \setminus B\) = neighbors of \(B\);
- \(D\) = \(V \setminus (B \cup C)\).

**Properties:** \(G[B]\) has only odd components; \(G[D]\) has only even components and contains a perfect matching; \(C\) is the minimizer of the Tutte-Berge formula (\(|C| = \mathrm{odd}(G-C) - \nu_{\mathrm{gap}}\)); every maximum matching matches \(C\) perfectly into distinct odd components of \(G[B]\).

The blossom algorithm's frustrated trees yield the Gallai-Edmonds partition algorithmically in polynomial time.

---

## Chapter 5: Polyhedral Combinatorics

### 5.1 Weighted Matching and LP Formulations

The **minimum weight perfect matching** problem asks: given \(G = (V,E)\) and costs \(c : E \to \mathbb{R}\), find a perfect matching \(M\) minimizing \(c(M)\).

The natural LP relaxation is:
\[
\min \sum_{e \in E} c_e x_e \quad \text{s.t.} \quad x(\delta(v)) = 1 \; \forall v \in V, \quad x \geq 0.
\]
This LP is **not integral** for general graphs because odd cycles create half-integer solutions. The fix is to add **odd-set constraints**:
\[
x(\delta(S)) \geq 1 \quad \forall S \subseteq V, \; |S| \text{ odd}, \; |S| \geq 3.
\]

<div class="theorem">
<strong>Theorem 5.1.1</strong> (Matching Polytope, Edmonds). The matching polytope of a general graph equals:
\[
P(G) = \{x \geq 0 : x(\delta(v)) \leq 1 \; \forall v, \; x(E(S)) \leq \lfloor |S|/2 \rfloor \; \forall S \subseteq V, |S| \text{ odd}\}.
\]
Every vertex of this polytope is the characteristic vector of a matching.
</div>

For **bipartite graphs**, the simpler LP (without odd-set constraints) is already integral by Birkhoff's theorem: the matching polytope equals the set \(\{x \geq 0 : x(\delta(v)) \leq 1 \; \forall v\}\), whose vertices are exactly matchings.

<div class="theorem">
<strong>Theorem 5.1.2</strong> (Birkhoff). Let \(G\) be bipartite. If the matching LP is feasible, its optimal value equals the cost of a minimum-weight perfect matching.
</div>

The proof constructs dual variables (vertex potentials) and iteratively updates them via a frustrated-tree procedure until complementary slackness is achieved — the classical **Hungarian algorithm** for weighted bipartite matching.

![Bipartite matching grows step-by-step via augmenting paths](/pics/co450/hungarian.gif)

### 5.2 Vertex Covers and LP Relaxation

<div class="definition">
<strong>Definition</strong> (Vertex Cover). \(U \subseteq V\) is a <strong>vertex cover</strong> if every edge has at least one endpoint in \(U\). The minimum vertex cover is \(\tau(G)\).
</div>

The LP relaxation of minimum vertex cover assigns \(x_v \in [0,1]\) to each vertex:
\[
\min \sum_v x_v \quad \text{s.t.} \quad x_u + x_v \geq 1 \; \forall uv \in E, \quad 0 \leq x_v \leq 1.
\]

![LP relaxation of vertex cover on a triangle: all vertices get x=1/2](/pics/co450/vertex-cover-lp.png)

On the triangle \(K_3\), the LP optimum is \(3/2\) (set each vertex to \(1/2\)) while the integer optimum is \(2\). This **integrality gap** of \(4/3\) shows the LP is not always tight for general graphs. However, for bipartite graphs the LP is always integral (follows from König's theorem via LP duality).

### 5.3 Totally Unimodular Matrices

Many important combinatorial LPs are integral because their constraint matrices are **totally unimodular (TU)**: every square submatrix has determinant in \(\{0, +1, -1\}\).

<div class="theorem">
<strong>Theorem 5.3.1.</strong> If \(A\) is a TU matrix and \(b\) is an integer vector, then the LP \(\min\{c^Tx : Ax \leq b, x \geq 0\}\) has an integer optimal solution whenever it is bounded.
</div>

![Totally unimodular matrix example with highlighted submatrix](/pics/co450/tu-matrix-example.png)

**Examples of TU matrices:**
- The incidence matrix of a **bipartite graph** (rows = vertices, columns = edges, each column has exactly two 1s, one in each side) is TU. This immediately implies the bipartite matching LP is integral.
- Network flow constraint matrices are TU.
- Interval matrices (rows = intervals, columns = points) are TU.

The TU property is the polyhedral explanation for why network flow, bipartite matching, and scheduling problems have polynomial-time exact algorithms via LP.

### 5.4 Cutting Planes and Integer Programming

When the LP relaxation of an IP is not integral, one adds **cutting planes** — valid inequalities that cut off the fractional LP optimum without removing any integer feasible point.

![2D integer program: LP relaxation region, integer points, and cutting plane](/pics/co450/cutting-plane.png)

The canonical example: maximize \(x + y\) subject to \(2x + y \leq 8\), \(x + 2y \leq 8\), \(x,y \geq 0\) integer. The LP optimum is \((8/3, 8/3)\) with value \(16/3 \approx 5.33\), but the integer optimum is \(5\) at \((3,2)\) or \((2,3)\). Adding the cut \(x + y \leq 5\) (derived from rounding down the LP value) eliminates the fractional solution.

<div class="key-box">
<strong>Cutting Plane Method</strong>

```
Solve LP relaxation; let x* be the optimal solution.
while x* is not integer:
    Find a valid inequality (cut) violated by x* but not by any integer feasible point.
    Add the cut to the LP and re-solve.
```

The **Gomory cut** is a systematic way to generate cuts from the LP simplex tableau. The **branch-and-cut** framework combines cutting planes with branching for modern integer programming solvers.
</div>

---

## Chapter 6: T-Joins, Flows, and Cuts

### 6.1 T-Joins

A **T-join** generalizes the concept of perfect matching to a parity condition on vertex degrees:

<div class="definition">
<strong>Definition</strong> (T-Join). Let \(T \subseteq V\) with \(|T|\) even. A \(T\)<strong>-join</strong> is a subset \(J \subseteq E\) such that the odd-degree vertices of \((V, J)\) are exactly \(T\).
</div>

The connection to matchings: the minimum-cost T-join (when \(c \geq 0\)) can be computed by finding shortest paths between pairs of vertices in \(T\), then computing a minimum-weight perfect matching on the complete graph \(G'\) with vertex set \(T\) and edge weights equal to shortest path distances.

**Key propositions:**
- \(J\) is a minimal T-join iff it is a union of \(|T|/2\) edge-disjoint paths connecting disjoint pairs in \(T\).
- The LP formulation of minimum T-join is: \(\min\{c^T x : x(\delta(S)) \geq 1 \; \forall T\text{-odd sets } S, \; x \geq 0\}\).
- This LP is integral (when \(c \geq 0\)), proven by a duality argument connecting T-join LP to the matching LP.

### 6.2 Maximum Flow and Min-Cut

<div class="definition">
<strong>Definition</strong> (Flow). For a directed graph \(D = (V, A)\) with capacities \(\mu \in \mathbb{R}_+^A\), an \(rs\)<strong>-flow</strong> is \(x \in \mathbb{R}^A\) satisfying flow conservation \(x(\delta^-(v)) = x(\delta^+(v))\) for all \(v \neq r, s\) and capacity constraints \(0 \leq x_a \leq \mu_a\).
</div>

<div class="theorem">
<strong>Theorem 6.1.1</strong> (Max-Flow Min-Cut, Ford–Fulkerson). \[\max\{f_x(s) : x \text{ feasible rs-flow}\} = \min\{\mu(\delta^+(R)) : \delta^+(R) \text{ is an rs-cut}\}.\]
</div>

The proof constructs the cut from the maximum flow: let \(R\) be vertices reachable from \(r\) by an \(x\)-incrementing path. Since \(s \notin R\) (maximality), \(\delta^+(R)\) is an rs-cut. Each forward arc in this cut is at capacity and each backward arc carries zero flow, giving \(f_x(s) = \mu(\delta^+(R))\).

**The Ford-Fulkerson Algorithm** repeatedly augments flow along shortest \(rs\)-paths in the residual graph. The **Edmonds-Karp** improvement (Dinits, 1970) shows at most \(nm\) augmentations suffice, giving \(O(nm^2)\) total runtime.

**Applications:**
- Bipartite matching reduces to max-flow (add source \(r\) connected to \(A\), sink \(s\) connected to \(B\), all capacities 1).
- The max-flow min-cut theorem gives a second proof of König's theorem: minimum cut in the bipartite flow network = minimum vertex cover.

### 6.3 Randomized and Approximation Algorithms

The **global minimum cut** can be found by repeatedly contracting random edges (Karger's algorithm). Each contraction merges two vertices; a minimum cut survives with probability \(\Omega(1/n^2)\), so repeating \(O(n^2 \log n)\) times and taking the minimum gives the global min-cut with high probability.

For **Set Cover** (cover all elements with minimum-cost sets): the greedy algorithm (always pick the set covering the most uncovered elements) achieves an \(H_n\)-approximation where \(H_n = \sum_{k=1}^n 1/k \approx \ln n\). This is tight (no polynomial-time \((1-\epsilon)\ln n\)-approximation unless P=NP). LP-based approaches via **randomized rounding** or **primal-dual** methods can achieve the same guarantee with different techniques.

---

## Summary of Key Theorems

| Theorem | Statement | Proof technique |
|---|---|---|
| MST optimality | Cycle and cut properties characterize MST | Direct exchange argument |
| Rado-Edmonds | Greedy optimal iff matroid | Counterexample for non-matroids |
| Jenkins | \(g \geq q \cdot \text{OPT}\) | Abel summation / telescoping |
| Matroid Intersection | \(\max = \min\) via augmenting paths | Augmenting path + reachability |
| Berge | Max matching iff no augmenting path | Symmetric difference argument |
| König | \(\nu(G) = \tau(G)\) for bipartite | Hall + alternating tree |
| Tutte-Berge | \(\nu(G) = \frac{1}{2}\min_A\{|V| - \text{odd}(G-A) + |A|\}\) | Induction + blossom contraction |
| Max-Flow Min-Cut | Flow value = min cut capacity | Residual graph reachability |
| TU integrality | TU + integer RHS \(\Rightarrow\) integer LP optima | Cramer's rule + half-integrality |

---

## Further Reading

- A. Schrijver, *Combinatorial Optimization: Polyhedra and Efficiency* (Springer, 2003) — the definitive reference, covering all topics in full generality.
- B. Korte and J. Vygen, *Combinatorial Optimization: Theory and Algorithms* (Springer) — accessible treatment of matroids, matchings, and flows.
- J. Edmonds, "Paths, Trees and Flowers" (1965) — the original blossom algorithm paper, a masterpiece of combinatorics.
- J. Oxley, *Matroid Theory* (Oxford University Press) — comprehensive treatment of matroids from foundations to advanced topics.
